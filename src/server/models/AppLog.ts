export default interface AppLog {
    id: number | null,
    log_type_code: string | null,
    log_type_text: string | null,
    log_content: string | null,
    log_time: string | null,
    lang: string | null
}